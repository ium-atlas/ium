한국어 | [English](CONTRIBUTING.en.md)

# 기여 가이드

IUM의 모든 콘텐츠는 `data/topics/`의 JSON 파일입니다. 코드를 몰라도 기여할 수 있습니다.

## 제1원칙: 출처 없는 서술은 받지 않습니다

이 프로젝트는 "여러 관점"을 다루기 때문에 편향 시비에 항상 노출됩니다. 유일한 방어막은 출처입니다.

- 모든 `narrative`에는 `sources`가 1개 이상 있어야 합니다
- 서술은 **"내 해석"이 아니라 "기록의 인용"**이어야 합니다.
  ✅ "이븐 알아시르는 ~라고 기록했다" / ❌ "아랍인들은 ~라고 생각했을 것이다"
- 출처 형식: `저자/기관, 문헌명, (권·페이지), (연도)` — 가능하면 1차 사료 또는 검정 교과서

## 비중(weight) 산정 기준

`weight`(0–100)는 "그 관점의 역사 서술 전통에서 이 사건이 차지하는 비중"입니다.

| 범위 | 의미 | 예시 |
|---|---|---|
| 90–100 | 그 문명권 역사 서사의 중심 사건 | 예루살렘 함락(유럽 95), 바그다드 함락(이슬람 100) |
| 60–89 | 주요 사건으로 상세히 서술됨 | 안티오크 공방전(유럽 80) |
| 30–59 | 언급되지만 비중은 제한적 | 니케아(아랍 35) |
| 10–29 | 간략히 기록됨 | 마라트 안누만(유럽 25) |
| 0–9 | 사실상 기록 없음 — **지도에 표시되지 않음** | 클레르몽 공의회(아랍 5) |

비중을 바꾸는 PR에는 근거(교과서 분량, 사료 서술량 비교 등)를 반드시 적어주세요.
당대 비중과 현대의 재해석 비중이 크게 다르면 `weight_modern` 필드를 추가할 수 있습니다.

## 기여 방법

### 1. 오류 제보·출처 제안 (가장 쉬움)
[이슈 템플릿](../../issues/new/choose)으로 제보해주세요.

### 2. 사건 추가/수정 PR
1. `data/topics/<주제>/events/`에 JSON 파일 추가 (스키마: `schema/event.schema.json`)
2. 파일명: `<사건id>.json` (소문자, 하이픈, 연도 포함 권장: `kuju-1231.json`)
3. 로컬 검증: `node scripts/validate.js`
4. PR 제출 — CI가 자동으로 스키마를 검증합니다

### 3. milestone 추가/수정 PR
`milestone`은 "같은 해, 다른 지역은 뭘 했나"를 보여주기 위한 한 줄 연대기 데이터입니다. 사건(event)보다 가볍지만 출처 규칙은 같습니다.

1. 지역별 파일 `data/milestones/<지역코드>.json`에 항목을 추가합니다. 지역 코드는 `data/milestones/regions.json`에 있어야 합니다
2. 항목은 `year`(정수, 기원전은 음수) 또는 `range`(`[시작, 끝]`, 시작≤끝) 중 정확히 하나만 가집니다
3. `label`은 필수이며 한 줄, 40자 이하여야 합니다. `note`는 선택입니다
4. `sources`는 필수이며 1개 이상이어야 합니다. 출처 없는 연대기 항목은 받지 않습니다
5. 로컬 검증: `node scripts/validate.js`

예시:

```jsonc
[
  { "year": 1592, "label": "임진왜란 발발", "sources": ["국사편찬위원회, 한국사연표"] },
  {
    "range": [1418, 1450],
    "label": "세종 치세",
    "note": "훈민정음 창제(1446)",
    "sources": ["국사편찬위원회, 한국사연표"]
  }
]
```

연도가 확실하지 않은 사건은 넣지 마세요. 확신 없는 권수·페이지를 쓰지 말고, 표준 통사·연표급 문헌이나 1차 사료를 우선 사용해주세요.

### 4. 새 주제 제안
이슈로 먼저 제안해주세요. 좋은 주제의 조건:
- 관점 간 비중 비대칭이 클 것
- 3개 이상의 관점에서 사료가 존재할 것
- 지리적 전개(경로·거점)가 있을 것

## 사건 유형 (type)

`battle`(전투) `siege`(공방전) `massacre`(학살) `council`(회의·선언) `political`(정치·즉위) `treaty`(조약·강화) `movement`(천도·이동) `culture`(문화·종교)

새 유형이 필요하면 이슈로 제안해주세요.

## 라이선스 동의

PR을 제출하면 기여한 데이터를 CC BY-SA 4.0으로, 코드를 MIT로 배포하는 데 동의하는 것으로 간주합니다.

## 분쟁 처리

- weight·명칭·서술을 둘러싼 이견은 이슈에서 **출처 기반으로만** 논의한다. 출처 없는 주장은 논의 대상이 아니다
- 합의가 안 되면 메인테이너가 최종 결정하고, **결정 근거를 해당 이슈에 기록**한다. 새로운 출처가 나오면 재논의할 수 있다
- 명칭 분쟁(예: 사건명이 관점마다 다른 경우): 각 관점의 narrative 안에서는 그 관점의 명칭을 쓰고, 공용 필드에는 해당 언어판에서 가장 통용되는 학술 명칭을 쓴다. 어느 쪽의 명칭도 지우지 않는다
- 합의 불가로 판정된 항목은 데이터에 표시한다: 이벤트 JSON에 선택 필드 `"disputed": { "fields": ["weight" 등], "issue": 이슈번호 }` 추가
- 현대 정치 논쟁의 투영 금지(기존 행동 강령 참조)

## 행동 강령

[CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)를 참고해주세요.
